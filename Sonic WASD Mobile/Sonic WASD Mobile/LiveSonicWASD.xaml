<Window x:Class="Sonic_WASD_Mobile.LiveSonicWASD"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live Sonic" Height="700" Width="440"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_Drag">

    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="5,5"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3300D4FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="25" BorderThickness="2" BorderBrush="#00D4FF" Background="#0A0A0A">
        <Border.Effect>
            <DropShadowEffect Color="#00D4FF" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="Transparent"/>

            <Border Grid.Column="1" Background="#121212" CornerRadius="0,22,22,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,10,0,0">
                        <Button Content="✕" Click="Close_Click" Style="{StaticResource SidebarButtonStyle}" Foreground="#FF4B4B" FontSize="14" Height="35"/>
                        <Button Content="▢" Click="Maximize_Click" Style="{StaticResource SidebarButtonStyle}" FontSize="14" Height="35"/>
                        <Button Content="—" Click="Minimize_Click" Style="{StaticResource SidebarButtonStyle}" FontSize="14" Height="35"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <Button Content="⛶" Click="ToggleFullScreen_Click" Style="{StaticResource SidebarButtonStyle}"/>
                        <Button Content="＋" Click="VolumeUp_Click" Style="{StaticResource SidebarButtonStyle}" FontSize="16"/>
                        <Button Content="－" Click="VolumeDown_Click" Style="{StaticResource SidebarButtonStyle}" FontSize="16"/>

                        <Button Content="⚡" Click="Connect_Click" Style="{StaticResource SidebarButtonStyle}"/>
                        <Button Content="🎮" Click="Mapping_Click" Style="{StaticResource SidebarButtonStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,0,0,15">
                        <Button Content="⚙" Click="Settings_Click" Style="{StaticResource SidebarButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
